name: WIRU Base Pipeline POC

on:
  push:
  workflow_call:
    inputs:
      environments:
        description: 'Environments: full-with-drp | full | mid | minimal'
        required: true
        type: string
      app-name:
        description: 'Application name'
        required: false
        type: string
        default: 'my-app'
      deploy-strategy:
        description: 'Strategy: rolling | blue-green'
        required: false
        type: string
        default: 'rolling'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ inputs.app-name }}
  cancel-in-progress: false

env:
  APP_NAME: ${{ inputs.app-name }}
  DEPLOY_STRATEGY: ${{ inputs.deploy-strategy }}

jobs:
  # === WIRU: INIT ===
  init:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Inicializar entorno (INIT)
        run: |
          echo "=========================================="
          echo "🚀 INIT"
          echo "=========================================="
          echo "📦 App: ${APP_NAME}"
          echo "🧭 Strategy: ${DEPLOY_STRATEGY}"
          echo "✅ Entorno inicializado"
          echo ""

  # === WIRU: CHECK ===
  check:
    needs: init
    runs-on: ubuntu-latest
    steps:
      - name: Validar configuración (CHECK)
        run: |
          echo "=========================================="
          echo "🔍 CHECK"
          echo "=========================================="
          echo "🌍 Environments: ${{ inputs.environments }}"
          echo "🔖 Branch: ${GITHUB_REF_NAME}"
          echo "✅ Configuración válida"
          echo ""

  # === WIRU: TEST ===
  test:
    needs: check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Detectar stack y ejecutar tests (TEST)
        run: |
          echo "=========================================="
          echo "🧪 TEST"
          echo "=========================================="
          if [ -f "package.json" ]; then
            echo "📦 Node.js detectado"
            npm ci || true
            npm test --silent || true
          elif [ -f "requirements.txt" ]; then
            echo "🐍 Python detectado"
            python -m pip install -r requirements.txt || true
            pytest -q || true
          else
            echo "ℹ️ Sin stack estándar detectado, ejecutando smoke test"
            echo "ok"
          fi
          echo "✅ Tests completados"
          echo ""

  # === WIRU: BUILD ===
  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build de la aplicación (BUILD)
        run: |
          echo "=========================================="
          echo "🏗️  BUILD"
          echo "=========================================="
          echo "📦 Compilando ${APP_NAME}..."
          # Placeholder: add npm run build / docker build / empaquetado
          echo "✅ Build completado exitosamente"
          echo ""

  # === WIRU: DEPLOY (DEV) ===
  deploy-dev:
    if: |
      inputs.environments == 'full' || 
      inputs.environments == 'full-with-drp' ||
      inputs.environments == 'mid'
    needs: build
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: Deploy a DEV (DEPLOY)
        run: |
          echo "=========================================="
          echo "🔧 DEPLOY TO DEV"
          echo "=========================================="
          echo "🚀 Desplegando ${APP_NAME} (strategy: ${DEPLOY_STRATEGY})..."
          echo "🌐 URL: https://dev.example.com/${APP_NAME}"
          echo "✅ DEV OK"
          echo ""

  # === WIRU: DEPLOY (STG) ===
  deploy-stg:
    if: inputs.environments != 'minimal'
    needs: [build]
    runs-on: ubuntu-latest
    environment: stg
    steps:
      - name: Deploy a STAGING (DEPLOY)
        run: |
          echo "=========================================="
          echo "🔨 DEPLOY TO STAGING"
          echo "=========================================="
          echo "🚀 Desplegando ${APP_NAME} (strategy: ${DEPLOY_STRATEGY})..."
          echo "🌐 URL: https://stg.example.com/${APP_NAME}"
          echo "✅ STAGING OK"
          echo ""

  # === WIRU: DEPLOY (PRD) ===
  deploy-prod:
    needs: [build]
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - name: Deploy a PRODUCTION (DEPLOY)
        run: |
          echo "=========================================="
          echo "🚀 DEPLOY TO PRODUCTION"
          echo "=========================================="
          echo "🚀 Desplegando ${APP_NAME} (strategy: ${DEPLOY_STRATEGY})..."
          echo "🌐 URL: https://prod.example.com/${APP_NAME}"
          echo "⚠️  Monitoreando post-deploy..."
          echo "✅ PRODUCTION OK"
          echo ""

  # === WIRU: DEPLOY (DRP) ===
  deploy-drp:
    if: inputs.environments == 'full-with-drp'
    needs: deploy-prod
    runs-on: ubuntu-latest
    environment: drp
    steps:
      - name: Deploy a DRP (Disaster Recovery)
        run: |
          echo "=========================================="
          echo "🛡️  DEPLOY TO DRP"
          echo "=========================================="
          echo "🚀 Desplegando ${APP_NAME} a sitio DRP..."
          echo "🌐 URL: https://drp.example.com/${APP_NAME}"
          echo "✅ DRP OK"
          echo ""

  # === WIRU: NOTIFY / SUMMARY ===
  summary:
    needs: [deploy-dev, deploy-stg, deploy-prod, deploy-drp]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Resumen del deployment (NOTIFY)
        run: |
          echo "=========================================="
          echo "📊 RESUMEN FINAL"
          echo "=========================================="
          echo "✅ Pipeline completado (con o sin skips)"
          echo "📦 App: ${APP_NAME}"
          echo "🌍 Environments: ${{ inputs.environments }}"
          echo "🔀 Strategy: ${DEPLOY_STRATEGY}"
          echo "⏰ Fecha: $(date -u +"%Y-%m-%dT%H:%M:%SZ")"
          echo "=========================================="
